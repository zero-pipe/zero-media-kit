RTP / FLV / MPEG-TS / MP4 / HLS / RTSP / RTMP 编解码与容器库（ZMK），**在本仓库维护**（非 [ireader/media-server](https://github.com/ireader/media-server) 官方仓库）。

| 目录 | 说明 |
|------|------|
| `libflv` … `librtsp` | 媒体容器与 RTP/RTSP/RTMP（ireader/media-server 系） |
| `libice` | WebRTC ICE/STUN/TURN（ireader/sdk 子集，供 ZMS `ZMS_ENABLE_WEBRTC` 使用） |

